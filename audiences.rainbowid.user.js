// ==UserScript==
// @name        咱要彩虹id
// @namespace   https://github.com/tomyangsh/userscrips
// @match       https://audiences.me/*
// @version     1.0
// @author      大統領
// ==/UserScript==

document.querySelectorAll('.Peasant_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.User_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.PowerUser_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.EliteUser_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.CrazyUser_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.InsaneUser_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.VeteranUser_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.ExtremeUser_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.UltimateUser_Name').forEach(node => node.className = 'Rainbow_Name');
document.querySelectorAll('.NexusMaster_Name').forEach(node => node.className = 'Rainbow_Name');
